# Extra dependencies for the DistilBERT transformer comparison.
# These are on top of the root invoice_classification/requirements.txt.

transformers>=4.40
accelerate>=0.30
datasets>=2.14
evaluate>=0.4

# Needed by HF Trainer's metric callbacks
scikit-learn>=1.3
