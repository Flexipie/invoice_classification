# Heavy / regeneratable artifacts — do NOT commit.
# Anything tracked from this folder should be code, docs, or small report files.

# DistilBERT checkpoints (~260 MB) and HF Trainer scratch dirs
models/
*.bin
*.safetensors
*.pt
*.pth

# Per-sample prediction dumps and training logs (recomputable; can be large)
reports/distilbert_test.json
reports/distilbert_training.json

# Misc
__pycache__/
*.pyc
.ipynb_checkpoints/
